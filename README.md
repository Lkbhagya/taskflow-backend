# TaskFlow Backend

Express.js backend for TaskFlow. Provides JWT authentication, user management, and task CRUD.

Quick start:

1. Copy `.env.example` to `.env` and adjust values.
2. Install dependencies:

```bash
npm install
```

3. Start server in development:

```bash
npm run dev
```

Default API base: `http://localhost:4000/api`
